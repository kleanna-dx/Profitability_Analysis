-- ============================================================
-- Migration: 027_pr86_to_pr88_no_schema_change.sql
-- Date    : 2026-06-04
-- Author  : AI Developer
-- Related : PR #86, PR #87, PR #88
-- ============================================================
--
-- ▶ 결론: 이번 배포(PR #86 ~ #88)에는 DB 스키마 변경 사항이 없습니다.
--          이 파일은 운영팀이 "027 번호에 해당하는 DDL 이 없다" 는 사실을
--          명시적으로 확인할 수 있도록 남기는 NO-OP(아무 동작 없음) 마이그레이션입니다.
--
-- ▶ 이번 배포에 포함된 변경 사항 요약
--   - PR #86 : [인터페이스 관리] 화면의 '월별 작업 추이' 차트를 배치관리 스타일의
--              단색 막대 그래프로 변경.            (HTML/CSS/JS 만 수정)
--   - PR #87 : [인터페이스 관리] 화면 상단 부제 문구 변경.
--              "SAP ↔ S&OP 인터페이스"  →  "SAP ↔ AI Analytics 인터페이스"
--              (HTML 문구 + server.mjs 주석 한 줄만 수정)
--   - PR #88 : '월별 데이터 현황' 데이터 소스를 [배치관리] 화면과 동일하게
--              bw_profitability_data 의 CALMONTH 별 행수로 통일.
--              (server.mjs 의 SELECT 쿼리만 변경, 스키마 변경 없음)
--
-- ▶ 사용된 테이블/컬럼은 모두 운영에 이미 존재하는 기존 자원입니다.
--      - 테이블 : bw_profitability_data        (이미 존재)
--      - 컬럼   : bw_profitability_data.CALMONTH (이미 존재)
--
-- ▶ 따라서 운영 DB 에 적용해야 할 DDL/DML 이 없습니다.
--      이 파일을 실행하셔도 무방하며, 실행해도 아무런 변경이 발생하지 않습니다.
--
-- ============================================================
-- (no-op section)
-- 멱등 실행 확인용 SELECT — 운영에서 실행 시 결과만 출력됩니다.
-- ============================================================
SELECT
    'PR #86~#88 : 스키마 변경 없음 (HTML/JS/서버 쿼리만 변경)' AS migration_note,
    NOW()                                                     AS checked_at;
