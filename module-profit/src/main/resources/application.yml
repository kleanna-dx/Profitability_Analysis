# ============================================================
# Module-Profit 독립 실행 설정
# ============================================================

server:
  port: 18083

spring:
  application:
    name: module-profit

  # MariaDB (integration DB - bw_profitability_data)
  datasource:
    url: jdbc:mariadb://10.2.14.247:3306/integration?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Seoul
    username: appuser
    password: Kleannara12#
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true

  # Spring Security - 기본 유저 비활성화
  security:
    user:
      name: admin
      password: admin

# ── SAP RFC 설정 ──
sap:
  rfc:
    ashost: "10.2.14.220"
    sysnr: "01"
    sysid: "BWP"
    client: "100"
    user: "ITM120"
    passwd: "kleannara123@"
    lang: "KO"
    rfc-function: "Z_BI_WEB_EX_BL"
    pool-capacity: 3
    peak-limit: 5
    schedule:
      enabled: true
      cron: "0 0 2 1 * *"

# ── 로깅 ──
logging:
  level:
    root: INFO
    com.company.module.profit: DEBUG
    com.company.module.profit.sap: DEBUG
